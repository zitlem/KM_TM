# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon?
._*

# Windows
Thumbs.db
Desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Logs / temp
*.log
*.tmp
*~

# Shop data — lives in the local/shared data folder, not in the repo.
# The tooling is sample data and ships with the app, so it stays tracked.
machines.json
settings.json
jobs/
